import React from "react";
import styled from "styled-components";

const NavContainer = styled.nav`
  width: 100%;
  height: 80px;
  border: 1px solid red;
`;

const Navbar = () => {
  return <NavContainer>testo</NavContainer>;
};

export default Navbar;
